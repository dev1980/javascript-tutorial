// Template Literals - ES6+
// Backtick characters `` - above tab (left from one)
// Interpolation ${} - insert expression(value)

const fname = 'john';
const age = 25;
const sentence = "Hey it's " + fname + ' and he is ' + age + ' years old';

const value = `Hey it's ${fname} and he is ${age} years old. And here is some simple math ${4 + 4}`;

console.log(value);

console.log(sentence);
